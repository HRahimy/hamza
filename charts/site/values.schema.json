{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "properties": {
    "namePrefix": {
      "description": "namePrefix will be used as a prefix for naming all resources (workload, network, storage, configuration) and labels generated by this chart.",
      "type": "string"
    },
    "imageRefUri": {
      "type": "string",
      "description": "imageRefUri is the full uri where the docker image can be accessed"
    },
    "deploymentNamespace": {
      "type": "string"
    }
  },
  "required": [
    "namePrefix",
    "imageRefUri",
    "deploymentNamespace"
  ]
}
